// build.rs
fn main() {
    // 只有在 Windows 平台下才编译资源文件
    if std::env::var("CARGO_CFG_TARGET_OS").unwrap() == "windows" {
        // 路径相对于项目根目录
        embed_resource::compile("resources/icon.rc");
    }
}